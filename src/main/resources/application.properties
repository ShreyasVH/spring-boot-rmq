server.port=${PORT}

spring.rabbitmq.host=${RMQ_IP}
spring.rabbitmq.port=${RMQ_PORT}
spring.rabbitmq.username=${RMQ_USERNAME}
spring.rabbitmq.password=${RMQ_PASSWORD}

queue=${RMQ_QUEUE_CONSUMED}
exchange=${RMQ_EXCHANGE_DIRECT}
routingKey=${RMQ_KEY_CONSUMED}

logging.level.org.apache.tomcat.util.net.NioEndpoint=OFF